// Hono app entry point: hostname router, static asset serving (architecture 7.1).
// TODO (Phase 2): resolve the hostname (studio, forms, demo, {client}, {property}),
// mount the routers below under /api, and fall through to the built React app.
