// In-browser API for Phase 1, mirroring every endpoint in architecture section 9.
// Same function names and return shapes as the live API in src/lib/api.js, so
// switching VITE_API_MODE changes no component.
export const mockApi = {
  mode: "mock",
};
