// Single API surface. VITE_API_MODE switches between src/data/mock and the live Worker endpoints (architecture 9). Components only ever import { api } from here.
// TODO (see architecture and README for the phase this belongs to).
export {};
