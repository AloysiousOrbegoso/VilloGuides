// Hostname resolution: subdomain routing in production, path-prefix routing in development (architecture 4, 14.1). Reserved subdomain list (4.3) and URL builders.
// TODO (see architecture and README for the phase this belongs to).
export {};
